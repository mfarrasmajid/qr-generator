<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <title>QR Code Generator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS (CDN) -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-7">
        <div class="card shadow-sm border-0">
          <div class="card-body p-4">
            <h1 class="h3 mb-3">QR Code Generator</h1>
            <p class="text-muted mb-4">Masukkan URL, lalu klik <em>Generate</em> untuk membuat QR Code dan unduh gambarnya.</p>

            <% if (error) { %>
              <div class="alert alert-danger"><%= error %></div>
            <% } %>

            <form id="qr-form" class="row g-3" novalidate>
              <div class="col-12">
                <label for="url" class="form-label">URL</label>
                <input type="url" class="form-control" id="url" name="url" placeholder="https://contoh.com" required>
                <div class="form-text">Gunakan protokol <code>http://</code> atau <code>https://</code></div>
              </div>
              <div class="col-12 d-flex gap-2">
                <button type="submit" class="btn btn-primary" id="btn-generate">
                  Generate
                </button>
                <a id="btn-download" class="btn btn-outline-secondary disabled" download="qrcode.png" href="#" role="button">
                  Download PNG
                </a>
              </div>
            </form>

            <hr class="my-4">

            <div id="preview" class="text-center">
              <div class="placeholder py-5 text-muted" id="placeholder">
                QR code akan tampil di sini.
              </div>
              <img id="qr-image" class="img-fluid d-none" alt="QR code">
            </div>

          </div>
        </div>
        <p class="text-center text-muted small mt-3 mb-0">Made with Express, QRCode, and Bootstrap.</p>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
          integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
          crossorigin="anonymous"></script>

  <script src="/public/js/main.js"></script>
</body>
</html>